# core Packages

if [ "${CBF['os_id']}" = 'alpine' ]; then
    declare PKGS='bash-completion coreutils openssh-client supervisor sudo ttf-dejavu unzip'
    package.checkIfInstalled "$PKGS"
    apk add --no-cache $PKGS 
fi